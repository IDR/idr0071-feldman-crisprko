---
continue: true
transfer: "ln_s"
exclude: "clientpath"
skip:
  - checksum
  - minmax
columns:
  - target
  - path
  - name

logprefix: "logs/"
output: "yaml"
parallel_upload: "10"
parallel_fileset: "10"
